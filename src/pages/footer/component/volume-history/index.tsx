import { defineComponent, ref, toRefs } from 'vue'
import './index.less'

export const VolumeAndHistory = defineComponent({
  name: 'VolumeAndHistory',
  setup() {
    return () => <div class="volume"></div>
  }
})
